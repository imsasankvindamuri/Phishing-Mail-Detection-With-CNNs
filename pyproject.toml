[tool.poetry]
name = "phishing-mail-detection-with-cnns"
version = "0.1.0"
description = "Capstone project adapting Zhu et al.'s PDHF hybrid CNN approach for phishing emails instead of URLs"
authors = [
    "V Sai Sasank <imsasankvindamuri@gmail.com>"
]
readme = "README.md"
packages = [
    {include = "src", from = "."}
]

[tool.poetry.dependencies]
python = "^3.12"
# Add your project dependencies here when needed
# tensorflow = "^2.13.0"
# scikit-learn = "^1.3.0"
# pandas = "^2.0.0"

[tool.poetry.group.dev.dependencies]
# Add dev dependencies here if needed
# pytest = "^7.4.0"
# black = "^23.0.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
phishing-detection = "src.main:main"
